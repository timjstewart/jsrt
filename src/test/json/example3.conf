# # The pattern that will locate the name of the function

# The pattern that will locate the name of the function
functionNamePattern = [].top.blocks.*

# The pattern that will locate the function body
exportPattern = **.code
